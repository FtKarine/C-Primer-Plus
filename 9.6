#include<stdio.h>
void rechnum(char ch);
int main()
{
    char ch1;
    while(scanf("%d",ch1)!=0)
        rechnum(ch1);
    return 0;
}
void rechnum(char ch)
{
    if(65<=ch<=90)
        printf("%d",ch-65+1);
    else if(97<=ch<=122)
        printf("%d",ch-97+1);
    else
        printf("-1");
}

#include<stdio.h>
double min(double x,double y);
int main()
{
    double i,j;
    printf("Please enter two numbers:\n");
    scanf("%lf %lf",&i,&j);
    min(i,j);
    printf("The bigger one is: %lf\n",min(i,j));
}
double min(double x,double y)
{
    double MIN;
    MIN=(x>y)?x:y;
    return MIN;
}
